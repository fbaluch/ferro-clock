export interface ClockPropsDebug {
  color: string;
  scale: number;
  showSegments: boolean;
  particleCount: number; // Simplified for debugging
  particleSize: number;
  particleSpread: number;
}

export {}; // Ensure this file is treated as a module
